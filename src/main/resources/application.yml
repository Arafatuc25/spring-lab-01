spring:
  application:
    name: spring-lab

server:
  port: 8080

app:
  owner: Aitkuzhinov Arafat
  group: IT1-2413IS
  date-pattern: yyyy-MM-dd HH:mm:ss
  mail:
    from: no-reply@iitu.kz
    retry-count: 3
    timeout: 5s
    enabled: true

logging:
  level:
    kz.iitu: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,env,configprops,conditions